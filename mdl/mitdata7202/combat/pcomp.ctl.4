@ERROR *
@TERM WIDTH 80
@DEFINE COMBAT: PS:<COMBAT>
@CONNECT COMBAT:
TOP::@TYPE plan.PLAN.-2
@IF (ERROR) @GOTO ENDJOB
@COMBAT:ZONE
@IF (ERROR) @GOTO CMPLST
@RESET ZONE
@CONNECT COMBAT:
@DELETE plan.PLAN.-2
@BACKTO TOP
CMPLST::@CONNECT COMBAT:
=<QUIT>
@IF (ERROR) @GOTO CMPLS1
CMPLS1::
@CLOSE
@IF (ERROR) @GOTO CNTCLS
CNTCLS::
@RESET
@RENAME plan.PLAN.-2 plan.LOST
@BACKTO TOP
ENDJOB::
